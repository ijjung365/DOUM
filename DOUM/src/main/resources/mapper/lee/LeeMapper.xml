<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.doum.mapper.lee.MyPageMapper">

    <!-- 게시물 작성 쿼리문 -->
    <insert id="saveStory">
        insert into TBL_BOARD
        values(#{boardId}, #{boardTitle}, #{boardContent}, 0, sysdate, sysdate, #{providerId})
    </insert>


    <!--    사용자 정보 조회 부분 -->
    <select id="selectUserById" >
        SELECT id, name, age, location, qualifications, available_days, bio, profile_image_url
        FROM users
        WHERE  id = #{id}
    </select>

    <!--    사용자 정보 저장 -->
    <insert id="saveUser">
        insert into
    </insert>

    <!--    봉사활동 이력 조회 부분 -->
    <select id="selectStoryId" >
        SELECT v.id, v.title, v.date, v.organization, v.image_url
        FROM volunteer_activities v
                 JOIN user_volunteer u ON v.id=u.volunteer_activity_id
        WHERE u.user_id = #{userId}
    </select>













</mapper>